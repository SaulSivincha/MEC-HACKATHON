#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

exec "$basedir/../../bin/esbuild"   "$@"
exit $?
# cmd-shim-target=/home/sauls/Dev/Proyects/DigitalTwin/node_modules/.pnpm/esbuild@0.28.1/node_modules/esbuild/bin/esbuild
